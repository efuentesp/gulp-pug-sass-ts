mixin button-xls(disabled=false, size="large")
  - var tooltip = ""

  if size == "large"
    button.button.big-button(id="btn_search" title=tooltip disabled=disabled)
        span.fa-stack.fa-lg
          i.fa.fa-file-excel-o.fa-2x
        br
        span XLS

  if size == "medium"
    button.button.medium-button(id="btn_search" title=tooltip disabled=disabled)
        span.fa-stack.fa-lg
          i.fa.fa-file-excel-o.fa-sm
  
  if size == "small"
    button.button.small-button(id="btn_search" title=tooltip disabled=disabled)
        span.fa-stack.fa-xs
          i.fa.fa-file-excel-o.fa-xs