mixin button-import(parms)
  - var _parms = parms ? parms : {size: "large", disabled: false, tooltip: ""}

  if _parms.size == "large"
    button.amButton.amBigButton(id="btnImport" type="submit" data-tooltip=_parms.tooltip disabled=_parms.disabled value="validate")
        span.fa-stack.fa-lg
          i.fas.fa-file-download.fa-2x
        br
        span Importar

  if _parms.size == "medium"
    button.amButton.amMediumButton.flex.items-center(id="btnImport" data-tooltip=_parms.tooltip disabled=_parms.disabled)
        span.fa-stack.fa-2x
          i.fas.fa-file-download.fa-sm
        span Importar

  if _parms.size == "small"
    button.amButton.amSmallButton(id="btnImport" data-tooltip=_parms.tooltip disabled=_parms.disabled)
        span.fa-stack.fa-sm
          i.fas.fa-file-download.fa-lg
