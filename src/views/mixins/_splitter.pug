mixin splitter(id, parms)
  - var _uppc_id = id[0].toUpperCase() + id.substr(1)
  - var _parms = (parms ? parms : {orientation: "vertical"})
  - var _class = "splitter-" + _parms.orientation

  .splitter(id="div" + _uppc_id class=_class)
    block