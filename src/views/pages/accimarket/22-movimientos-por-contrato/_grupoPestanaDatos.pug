include /mixins/mixins

+tab-group("grupo_datos",
    {id: "tab_datos", label: "Datos del Contrato"},
    {id: "tab_filtro", label: "Filtro de movimientos"})
    +tab-item("tab_datos")
        .row
            +field-input("cliente",
                {
                    label: "Cliente:",
                    size: "48em",
                    disabled: true,
                    orientation: "horizontal"
                })
        .row
            +field-input("digito",
                {
                    label: "Dígito:",
                    disabled: true,
                    orientation: "horizontal"
                })
            +field-input("digitoVer",
                {
                    label: "Dígito Verif:",
                    disabled: true,
                    orientation: "horizontal"
                })
        .row
            +field-input("perfil",
                {
                    label: "Perfil:",
                    size: "15em",
                    disabled: true,
                    orientation: "horizontal"
                })
            +field-input("libro",
                {
                    label: "Libro:",
                    size: "15em",
                    disabled: true,
                    orientation: "horizontal"
                })
        .row
            +field-input("status",
                {
                    label: "Status:",
                    disabled: true,
                    orientation: "horizontal"
                })
            +field-input("area",
                {
                    label: "Area:",
                    disabled: true,
                    orientation: "horizontal"
                })
            +field-input("division",
                {
                    label: "División:",
                    disabled: true,
                    orientation: "horizontal"
                })
            +field-input("direccion",
                {
                    label: "Dirección:",
                    disabled: true,
                    orientation: "horizontal"
                })
        .row
            +field-input("region",
                {
                    label: "Región:",
                    disabled: true,
                    orientation: "horizontal"
                })
            +field-input("sucursal",
                {
                    label: "Sucursal:",
                    disabled: true,
                    orientation: "horizontal"
                })
            +field-input("zona",
                {
                    label: "Zona:",
                    disabled: true,
                    orientation: "horizontal"
                })
            +field-input("gerencia",
                {
                    label: "Gerencia:",
                    disabled: true,
                    orientation: "horizontal"
                })
    +tab-item("tab_filtro")
        .row
            label.ml-4.font-bold Emision:
            label.ml-48.font-bold Operacion:
            label.ml-48.font-bold Portafolio:
        .row
            label.ml-4 --TODOS--
            label.ml-48 --TODOS--
            label.ml-48 --TODOS--